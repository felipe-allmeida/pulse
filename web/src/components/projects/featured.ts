/**
 * The slug of the featured project — rendered wide with an ambient glow on
 * the index grid and the detail page's screenshot slot. `pulse` is this
 * site itself: the one project visitors can watch running live, so it earns
 * the emphasis.
 */
export const FEATURED_PROJECT_SLUG = 'pulse';
